console.log(JSON.stringify({
  status: "pending-human-review",
  evidence: "Run the real-browser smoke flow at 375px and desktop width; confirm keyboard, validation, API error, success, reset, and reduced-motion behavior, then obtain Emerson's visual approval."
}));
